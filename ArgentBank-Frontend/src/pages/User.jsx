// Page tableau de bord utilisateur (dashboard)
// Correspond au fichier "user.html" du dossier Frontend-Ref.
// Cette page sera protégée : accessible uniquement si l'utilisateur est authentifié.
// Elle affichera les comptes bancaires + bouton pour éditer le profil.

export default function User() {
  return (
    <main>
      {/* TODO : Intégrer la structure HTML/CSS depuis user.html */}
      <h1>User Dashboard</h1>
      <p>Données utilisateur à afficher ici (via Redux + API).</p>
    </main>
  );
}
